name: Hello World

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo 'Hello World'
      - name: Now it is
        run: date
      - name: Directory content
        run: ls -l
